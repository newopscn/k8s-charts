apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "strongswan.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "strongswan.name" . }}
    helm.sh/chart: {{ include "strongswan.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "strongswan.fullname" . }}:500']
  restartPolicy: Never
